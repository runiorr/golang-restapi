version: "3.9"
services:
  postgres:
    image: postgres:alpine
    restart: always
    user: postgres
    environment:
      PGUSER: guest
      POSTGRES_PASSWORD: guest
      POSTGRES_DB: goapi
  api:
    build: .
    command: ./main
    environment:
      - PORT=:3000
    expose:
    - "3000"
    depends_on:
      - postgres
    deploy:
      replicas: 2
  nginx:
    image: nginx:1.23.3
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api
  